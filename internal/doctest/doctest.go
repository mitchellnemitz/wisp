// Package doctest holds the documentation tests. They compile and run the
// example programs and the wisp code blocks embedded in the user docs, so
// neither can drift from the compiler. There is no runtime code here.
package doctest
