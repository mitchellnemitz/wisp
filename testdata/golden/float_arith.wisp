fn main() -> int {
  let a: float = 3.14
  let b: float = 2.0
  print(to_string(a + b))
  print(to_string(a - b))
  print(to_string(a * b))
  print(to_string(a / b))
  print(to_string(-a))
  return 0
}
