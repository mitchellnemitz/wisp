{
  "desc": "read_secret: hostile metacharacters in input are inert -- IFS= read -r prevents command substitution, glob expansion, and field splitting",
  "stdin": "$(touch PWNED); `id`; *\n",
  "stdout": "$(touch PWNED); `id`; *\n",
  "exit": 0,
  "stderr": { "contains": "pw: " }
}
